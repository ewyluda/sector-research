# TODO

Cross-session task tracker. Context for each item is in `docs/superpowers/specs/2026-04-14-data-gaps-analysis.md` unless noted otherwise.

## In progress

- _Nothing in flight._ **The 2026-06-10 improvement campaign is COMPLETE** (all 8 blocks; PRs #41-#51, #53). Loose end parked: architecture.json touch-up for B3 phases 2-4. (FMP key rotation: DONE 2026-06-11 — user regenerated at the FMP dashboard, new key verified live; old git-history-exposed key is dead.)

## Backlog / polish

- _Empty._ Pick from Backlog / v3 or one of the deferred refactors when ready.

## Next up (sequenced)

- _No sequenced next steps. Pick from the Backlog / polish or v3 list when ready._

## Backlog / v3

- Interactive D3 force-directed full-graph viewer.
- Sankey revenue-flow visualization for supply chain.
- Graph centrality (betweenness, eigenvector) as an input to discovery ranking.
- Options IV / put-call / short interest — new vendor or FMP higher tier.
- Credit ratings — new vendor.
- Institutional ownership (13F) via FMP — current plan returns 404 on ticker-side endpoints; would need daily polling of `institutional-ownership/latest` into a local ticker→holder aggregation.

## Done (recent)

- **Congressional-trading signal (2026-06-11)** — third insider-adjacent signal, premise revised: Capitol Trades' unofficial API is dead (CloudFront 503), built on FMP `/stable/senate-trades` + `house-trades` instead (live-verified on the current plan; official efdsearch/clerk.house.gov links per row). New `congress_transactions` table (migration `c4d8e91f72a6`; sha256 natural key over parsed values incl. `owner` — Self/Spouse same-day lines distinct; coarse amount ranges mean genuinely identical same-day lines collapse, accepted) + `congress_ingest.py` (amount-range midpoint parser, open-ended ranges use the lower bound) + pure `congress_signal.py` (90-day aggregate, net value from `amount_mid` midpoints, cluster = ≥2 distinct politicians in 30d, modifier +3/+1/−2 — half-ish of insider's due to ≤45-day disclosure lag). Wired into the daily material scan fault-isolated AFTER the insider commit so a congress FMP failure rolls back only its own rows; `_persist_insider_signal` generalized with a `signal_type` param → `signals` `signal_type="congress"` + history dual-write. Discovery: shared `_apply_cached_modifier` (insider path delegates, zero behavior change), `CongressSnapshot` on the card, congress chip on theme detail. Live smoke on NVDA: 200 rows ingested, idempotent re-ingest (added=0), cluster +3 computed. 47 new tests (18 signal + 16 ingest + 4 FMP client + 3 scheduler + 6 discovery); suite 906 green; all frontend gates green.

- **Citation dedupe on risk-loop re-runs (2026-06-11)** — `ResearchState.add_citation` now dedupes on `(source_url, metric)`: a duplicate key replaces the existing entry in place (latest fetch wins, stable position) instead of appending, so loop-back deep-dive re-runs no longer mint duplicate chips in the report CitationList. Single seam — all 7 call sites route through `add_citation`; dict-form citations dedupe too. Note: states persisted before this fix keep their historical duplicates (no render-time pass — re-run if it matters). 6 new tests in `test_state_citation_dedupe.py`; suite 859 green.

- **Fill-in polish pack (2026-06-11)** — six parked items closed. `useWorkspaceKickoff` hook replaces the 3 copied kick-off closures (WorkspaceButton / ReportHeader / RetryRunButton; ReportHeader's loading state → hook `busy`). ValidationCard adapters typed (`SensitivityGrid` / `ReverseDcfResponse["thesis_vs_priced_in"]`) — last 2 workspace-UI `any`s gone. DifferentiationCard read-through badge human-readable (label + event_date; raw event_key → tooltip; payload description shown). lib/api placement: 13th domain module `peers.ts` (peersApi + 4 types out of workspace.ts, barrel-exported, zero call-site changes), `getSignalHistory` → themes.ts, fmt helpers → `lib/format.ts` (2 importers updated). FMP citations persisted: `unwrap_gather_citation` helper + node_deep_dive wiring — all 10 primary + 6 tier-2 FMP citations land in `state.citations` (was transcript + FRED only); loop-rerun dedupe parked in Backlog. Correlated metric guard (tier 3 in `metric_guards.py`): impossible-gross corruption also nulls ebitdaMarginTTM + EV/EBITDA (both builders) + Net Debt/EBITDA (overview); stale test pin flipped, MSFT clean-control added. Suites: backend 853, frontend node 32; all gates green.

- **Post-campaign fix pack (2026-06-11)** — three daily-use dead ends closed. (1) Issue #52: `GET /api/earnings/board` now surfaces past prints with null actuals as `post_pending` (extracted `_choose_print` helper, precedence post > post_pending > pre; pinned by `backend/tests/test_earnings_board_selection.py`); status board shows an amber `pending` badge and the EarningsDrawer renders estimates with an awaiting-actuals note — the `expand_earnings` deep link works the morning after a print. (2) Seed-only 8-K dead end: `OrphanEventsSection` on /status renders material events for tickers with no board row (`deriveOrphanEvents` in `lib/orphanEvents.ts`, node-tested); `?expand_events=` auto-opens orphan rows; dismiss works via the existing handler. (3) Snoozed-question visibility: `?status=snoozed` virtual filter + idempotent `POST /api/questions/{id}/unsnooze` (409 on non-open), Snoozed view on /questions with per-row "Snoozed until" chip + Unsnooze button. Suites: backend +11 tests, frontend +4 node tests; all gates green.

- **B3 phases 2-4 — UX overhaul complete (improvement campaign, 2026-06-11, PRs #50, #51, #53)** — Phase 2 (IA): `GET /api/tickers`; global ⌘K `GlobalCommandPalette` in the root layout (tickers/surfaces/runs/contextual actions/report sections; report-local palette deleted; `sections.ts` stays the single registry); nav slimmed to 6; Today gains a Briefing/Calendar tab strip absorbing Catalysts (`/catalysts` → server redirect; Next 16 `useSearchParams`-needs-Suspense footgun handled, `/` stays static); attention severity tiers (TDD'd), per-ticker undated-catalyst compaction, archived-thesis chips (catalyst rows only — earnings rows can't distinguish archived from never-thesis); ProspectusList on /filings; workspace Retry; status row menu (Company/Model/Questions) + KillCriteriaDrawer (optimistic toggle, criteria text from the run report — the KC endpoint carries no text). Phase 3 (lists): questions `snoozed_until` (migration `a1ea628ef06d`) + `POST /questions/bulk` + URL-state filter chips/bulk bar; 8-K near-duplicate grouping (`material_events_grouping.py`, ×N badges, group dismiss); `POST /runs/{id}/abandon` + Library grouped-by-ticker rebuild (gate-era chips retired; backfills: 6 zombie runs + 14 gate-era `awaiting_approval` rows on both DBs); filings polish (chore chip via `FilingsWorkbench` callback-lift, depth-2 graph default, wording). Phase 4: 9-fix bundle (EventCard dedupe, rollup deep links, signals-refresh banner with partial-failure copy, ticker-everywhere links, copy fixes, reverse-DCF no-solution footnotes, n/a-vs-missing grid cells, Researched chip — `CompanySignalCard.last_run_id` was a never-populated dead field, now backed by a real runs-list map); theme diagnosis (light-render mechanism GONE — dark tokens landed post-audit; removal no-op) + 93-hit token sweep (5 per-surface commits, screenshot parity, 12 justified survivors, AGENTS.md tokens-only guard). Acceptance walk: 4 B1 journeys re-walked live — 2 / 3 / 3 / 2 actions vs B1's ~5 / ~5 / 6-step-detour / dead-end; 7/8 §14 spot-checks pass (1 pre-existing gap → issue #52). Suites: backend 836, node 28; every phase spec+quality reviewed; CLAUDE.md reconciled.

- **C2 frontend + C3 closure (improvement campaign, 2026-06-11, PRs #48 + #49)** — executed in the window right after B3 phase 1 (PR #44) merged, while no UX branch was in flight. PR #48 (M2.3+M2.4): `frontend/lib/api.ts` (2,451 lines) → 12 domain modules under `lib/api/` (`core`/`themes`/`filings`/`pipeline`/`catalysts`/`status`/`workspace`/`performance`/`transcripts`/`model`/`prospectus`/`company`) with `lib/api.ts` as the barrel — zero call-site changes, all 244 exports preserved (verified mechanically via the TS compiler API), `BASE`/`apiFetch` package-private via selective core re-export; the three workspace-SSE `any`s replaced with honest types (`WorkspaceStepOutput` 5-way union, `Citation[]`, `ReadThroughItem[]` — field-verified against backend schemas; casts proven sound; `WorkspaceReport` state now `Partial<Record<WorkspaceStep, StepResult>>`, cast-free) — typing exposed dead code: `DifferentiationCard` rendered a never-existing `rt.summary`. PR #49 (M3.5/M3.7/M3.8): CLAUDE.md/CONTEXT.md reconciled vs PRs #42-48 (11 drift items fixed — preflight warning + model-skip, curator_private dismiss routes, metric-guard policy, barrel description, prospectus replay, suite counts); `Theme.seed_tickers` JSDoc documenting the JSONB mirroring gap (reads serve rows as-is; only writes normalize); `design/architecture.json` refreshed to main `b7ea0e7` + 6 stale spots in `architecture.html` fixed. Campaign blocks C2 and C3 both ✅. Note: PR #49's M3.5 sweep predates PR #44's done-log landing — the `prior_ticker_model: TickerModel | None` resolution is reflected in the preflight prose already.

- **B3 phase 1 — UX unblocks (improvement campaign, 2026-06-11, PR #44)** — first of four B3 phases (plan: `docs/superpowers/plans/2026-06-10-ux-overhaul.md`, local-only). **Earnings-day unblock:** `no_ticker_model` demoted to a non-blocking preflight `warnings[]` code (other five reasons still block); `kick_off` + `step_update_refresh` handle `prior_ticker_model=None` (version-0 sentinel, `model_skipped` on `UpdateRefreshOutput`, EDGAR new-filings still surfaced via extracted best-effort `_fetch_new_filings`); WorkspaceButton shows visible blocked reasons + CTA (no more title-tooltip), UpdateRefreshCard "Create model →", report header "no model" chip, ValidationCard neutral skip copy (current_price=0 is overloaded: no-model AND no-live-price). **Curation dismiss:** `curator_private` tombstone rows in `counterparty_aliases` (migration `77bcb5d1bfbd` — already on main via C1's chain copy; this PR adds the matching ORM nullability), honored by BOTH resolvers + excluded from the write-back cascade; idempotent double-dismiss; dismiss/undo/list endpoints + CurationPanel UI with always-visible pending count. **Metric guards (premise revision):** live FMP dumps disproved the audit's "scaling bug" — ORCL Gross 118.1% is upstream FMP corruption (negative costOfRevenue, FY2026 Q4), −5911.7% is a faithful tiny-base growth value, "Rev 5Y CAGR" was FMP's cumulative figure; fix = two-tier `metric_guards.guard_margin` (impossible gross>1.0 nulled+warned; others warn-only in [−5,1.5] so CORZ's real −343% net margin passes), CAGR annualization, `pct_growth` unit rendering n/m beyond ±1000%, tiny-base delta suppression in peer comp; conventions pinned by recorded-payload tests. **Performance:** `populated_offsets` on the summary; default = largest offset with data (URL wins); "too young" annotation; superseded hidden behind a toggle; By-verdict split into Run-verdict / Workspace-health sections. Every task two-stage-reviewed (spec+quality) with fixes; live Playwright walk on the test DB passed all 5 acceptance checks (one-click model-less VRT workspace run end-to-end). Merge conflict vs C3's M3.2 typing resolved as `prior_ticker_model: TickerModel | None` — M3.2's "non-nullable justified by raising _preflight" rationale is obsolete as of this PR. Suite 807 green post-merge. Known follow-up: ORCL stats outside the guard list (EV/EBITDA 113×, EBITDA margin 9.2%) still show the upstream corruption.

- **M3.5 doc slice + M2.1 characterization tests (improvement campaign, 2026-06-11, PR #47)** — CONTEXT.md no longer lists the `compute_delta` race as a Known limitation (fixed in `18e601f`; guard verified present in `services/transcript_delta.py` before rewriting the doc). M2.1 (the audit Milestone-2 row never sequenced into a campaign block): 56 new characterization tests — `test_discovery_scoring.py` (26: fresh 40/40/20 weighting with hand-computed literals, missing/stale collapse to 80/20 — staleness honestly attributed to the `_merge_results` caller, custom `signal_weights` overrides with `.get` fallbacks, all-missing → true 0.0 no floor, the ×1000 fresh-path vs ×100 collapse-path discovery-normalization asymmetry, ROIC tiers, velocity caps) and `test_fmp_parsers.py` (30: top-5 FMP methods vs empty/None/error-dict/wrong-type/missing-key payloads; pinned footguns documented inline — error dicts pass through verbatim to callers expecting lists, explicit `price: null` bypasses the `.get` "N/A" default, zero type coercion anywhere). Suite 766 green; zero production changes.

- **C3 backend half (improvement campaign, 2026-06-11, PR #46)** — M3.1: `run_transcript_analysis` returns a frozen `TranscriptAnalysisResult` (`status: ok|no_data|error`, value, error); `node_deep_dive` unwraps so the persisted `state.transcript_analysis` JSONB shape is unchanged (report API / SSE / frontend unaffected); per-pass partial failures still classify as ok; structural failures log with traceback in the service. M3.2: `WorkspaceContext`'s five `Any` fields → real `TYPE_CHECKING` annotations (`FMPClient`/`EdgarClient`/`AsyncAnthropic`/`ResearchRun`/`TickerModel`; non-nullable justified by the raising `_preflight`). M3.4: both outcome-tracker snapshot builders warn-log (with ticker/run_id) and stamp `"incomplete": true` when state fields are missing OR corrupt non-dict; absent key = complete so old snapshots stay valid; `SignalSnapshot` ignores the extra key. M3.6: `_fmt_fundamentals` → 12 per-section helpers + thin composer; byte-identity pinned by `test_fmt_fundamentals_identity.py` (frozen legacy copy — labeled do-not-fix; deviations limited to stripped docstrings + inlined `_first_metric`). Prospectus SSE migrated to the A2 multi-subscriber replay pattern (4 new tests; all three SSE services now consistent). M3.7 delete-half: `design/style-explorer.html` + `citation-styles.html` removed, stale comment refs cleaned (CitationBlock JSDoc, globals.css). Suite 710 green; frontend gates clean.

- **C2 backend half — nodes.py split (improvement campaign M2.2, 2026-06-11, PR #45)** — `graph/nodes.py` 1,669→888 lines via pure moves: `graph/formatters.py` (599 — `_extract_score`/`_extract_key_findings`/`_first_metric`/`_fmt_fundamentals`/`_build_curated_financials`/`_build_technical_data`; pure transforms, no LLM/DB), `services/transcript_analysis.py` (98 — `run_transcript_analysis`; note: patch `llm.complete` in tests, not this module — `complete` is imported function-locally), `services/question_lifecycle.py` (165 — question persist/fetch/render helpers). All 14 externally-consumed symbols re-exported from `nodes.py` as a documented transitional shim (consumer list in the docstring; migrate + delete later). Zero test files modified; suite 691/691; AST-level body diff verified the moves are pure. CLAUDE.md location refs updated incl. a pre-existing stale "nested closure" claim about counterparty context.

- **C1 perf quick-wins pack (improvement campaign, 2026-06-11, PR #43)** — six-fix bundle, all premises re-verified live first. (1) `React.memo` on 14 deep-dive sections PLUS `frontend/lib/categoryWrappers.ts` (useRef-cached, reference-stable category wrappers keyed on `CategoryState` identity — without it the pipeline page minted fresh wrapper objects per SSE event and memo never fired; 6 node tests pin wrapper identity); `DeepDiveDashboard` gained `"use client"` and imports `CategoryState` from the helper. (2) 30s debounce on visibility-triggered refetches for all 4 status-page pollers (board / read-throughs / earnings / material-events); interval + filter-change paths untouched; stamps are success-only. (3) 30s TTL module cache on `models_api._fetch_live_price` (never caches 0.0; keyed on TickerPath-normalized upper tickers; live-verified 2 reverse-DCF opens → 1 FMP `/quote`). (4) `archived_at` threaded `StatusBoardEntry` → `StatusBoardEntryOut`/`_serialize_entry` → `lib/api.ts` → status page; the include-archived double-fetch + run_id-diff hack deleted (live archive→ISO→unarchive round-trip verified). (5) discovery `_merge_results` SurpriseAlert N+1 → single `IN()` + set membership (test pins exactly one query; tolerates multi-alert rows where old `scalar_one_or_none` raised). (6) migration `eb21c3df2ad5`: partial index `ix_research_runs_status_board_latest (ticker, theme_id, created_at DESC) WHERE status IN ('completed','watchlist') AND theme_id IS NOT NULL` — the planned COALESCE-expression index is impossible (`::timestamptz` cast is STABLE not IMMUTABLE); predicate exactly matches `_build_latest_runs_sql`; reversible; also carries a byte-identical copy of `77bcb5d1bfbd` (alias tombstones) from the in-flight ux branch to keep the alembic chain. Backend suite 691; frontend lint/typecheck/build/test clean.

- **A2 correctness fixes (improvement campaign, 2026-06-11, PR #42)** — M1.2 background pipeline tasks open their own `async_session()` (request-scoped sessions no longer cross into `asyncio.create_task`; 3 call sites in `api/pipeline.py` + `services/pipeline.py`). M1.3 single-source phase routing: `PHASE_SEQUENCE` + `next_phase()` in `graph/pipeline.py`, `after_*` conditional edges lifted to module level and derived from the table, `services/pipeline.py::_next_phase` delegates; `test_phase_routing.py` (28 tests) pins both sides against drift (caveat documented in-code: the `Literal[...]` edge annotations still hardcode targets — LangGraph reads them statically). M1.4 SSE multi-subscriber: pipeline `_streams` → `dict[run_id, list[Queue]]` fan-out with own-queue unsubscribe (pre-subscribe events still drop — REST hydration recovers them); workspace mirrors with a per-run replay buffer (pre-subscribe buffering load-bearing there since kick_off emits before the frontend connects); subscriber queues bounded at 500; `test_sse_streams.py` (11 tests incl. client-disconnect cancellation cleanup). Suite 641→680 green; ruff clean; live E2E run on the test DB completed through a deep-dive loop-back with two concurrent SSE tabs receiving all events independently. Known leftover: `prospectus_service.py` still uses the legacy single-queue SSE pattern (→ C3 candidate).

- **UX/IA design spec (improvement campaign B2, 2026-06-10)** — brainstormed with the user from the B1 audit; spec at `docs/superpowers/specs/2026-06-10-ux-overhaul-design.md` (local-only), user-approved. Locked: dark theme everywhere via token-first mechanical sweep (reconcile the dual `--x`/`--color-x` namespaces; diagnose why workspace report / model forecast / company peers resolve light); top-nav slimmed to 6 entries (Today gets a Briefing/Calendar tab strip absorbing Catalysts; Prospectus/Workspace/Questions demoted but kept live); global ⌘K generalized from the report CommandPalette (tickers/surfaces/recent runs/actions, report sections merge in); earnings-day unblock by demoting `no_ticker_model` to a preflight warning + `update_refresh` model-skip; curation-queue `curator_private` alias tombstones; questions `snoozed_until` + bulk endpoint; Library grouped-by-ticker + `abandoned` run status; API-layer 8-K dedup; Performance latest-offset default + taxonomy split; FMP ratio-scaling fix pulled in from Track C; 9-item S bundle. No code changes (design session). Next: B3 implementation plan.

- **A1 safety net (improvement campaign, 2026-06-10, PR #41)** — GitHub Actions CI (`.github/workflows/ci.yml`: backend ruff + unittest via explicit enumeration with dummy env vars; frontend `tsc --noEmit` + eslint + `node --test lib/*.test.mts`; actions at Node-24-ready majors; verified green 3× on branch + on main). httpx apikey log redaction (`backend/app/logging_filters.py`, installed on the httpx logger AND root handlers in `main.py` — also catches the FMP retry-warning leak via HTTPStatusError messages). Discovery `_extract_*` helpers now `logger.exception` instead of silent None (+ empty-income guard on `_extract_gross_margin`). Smoke/verify scripts converted to unittest: `test_dcf`/`test_model_balancing`/`test_reverse_dcf`/`test_model_diff` (+ shared `backend/tests/model_fixtures.py`), `test_parser_*` ×5, `test_status_board_sql`; 10 scripts deleted, remaining manual scripts documented in `backend/scripts/README.md`. Characterization tests (`test_output_parsing.py`, 24 tests) pin `_extract_score` silent-50 fallback, digit-bullet mangling, and `parse_structured_output` edges ahead of the nodes.py split. Ruff baseline (default rules, `ruff.toml` + `backend/requirements-dev.txt`): 89 errors fixed or explicitly per-file-ignored, zero behavior change (reviewed hunk-by-hunk). Backend suite now 641 tests. `hunches.md` leaked FMP key REDACTED — **key rotated 2026-06-11** (user regenerated at the FMP dashboard, new key verified live; the git-history-exposed key is dead).

- **UX discovery audit (improvement campaign B1, 2026-06-10)** — walked all 28 `app/**/page.tsx` surfaces live with Playwright (real DB data, drawers/modals/deep-links exercised); audit at `docs/superpowers/2026-06-10-ux-audit.md` with 32 screenshots in `docs/superpowers/ux-audit-2026-06-10/`. 21 ranked recommendations (S→L). Top findings: earnings-day journey blocked by hidden `no_ticker_model` workspace preflight (disabled ↻ button, tooltip-only reason); counterparty curation queue (50 pending) has no dismiss action for private companies; route-level light/dark theme split (workspace report + model forecast tab + company peers tab render CSS-variable light, rest hardcoded dark); EventCard renders catalyst titles twice; FMP ratio-scaling bugs on company StatisticsGrid (Gross 118.1%). No code changes (discovery session). Executed before A1 at user direction — A1/A2/C1 still pending.

- **Trade journal (investor-portal roadmap sub-project 6, 2026-06-10)** — `journal_trades` table (migration `a2c6676dd30d`, nullable FK to `verdict_outcomes` SET NULL). `services/journal.py` is commit-free (callers own session); FMP adjusted-close auto-fill with 7-day on-or-before lookback + best-effort SPY benchmarks; date-move refresh of FMP-sourced fills only (manual prices sticky). `services/journal_comparison.py` is pure: direction-aware fractional returns (short = −long), SPY excess = trade − SPY over holding period, nearest-offset snapshot comparison (midpoint thresholds 4/18/60/136 days), summary rollups. `/api/journal` router: trades CRUD, PATCH-close/reopen semantics (explicit `exit_date: null` reopens + clears exit fields), `summary`, `price-preview` (FMP adjusted close on-or-before, 7-day lookback), `link-candidates` (non-superseded outcomes by ticker). `journalApi` in `frontend/lib/api.ts`; journal section on `/performance` (`components/journal/`: TradeJournalSection, TradeForm, TradeList, DecisionVsOutcomePanel, ExitReasonTable); one-shot `?log_trade=TICKER` deep link; Log-trade buttons on status board + company header. FMP failures degrade gracefully (SPY columns null, preview 404, no 500s). 41 new tests (12 comparison + 15 service + 14 API); full suite green; e2e smoke verified. Spec: `docs/superpowers/specs/2026-06-10-trade-journal-design.md`; plan: `docs/superpowers/plans/2026-06-10-trade-journal.md` (local-only).

- **Deterministic quant layer (roadmap #5)** — `services/quant_fingerprint.py` (pure: Piotroski F, Altman Z, Beneish M, accruals, FCF conversion, SBC dilution, margin OLS slopes; TTM vs prior-TTM over the 8 quarters node_deep_dive already fetches). Attached to `CuratedFinancials.quant_fingerprint` (zero new API surface), routed into 5 category prompts via `QUANT_ROUTING` + `{quant_data}` slot ("established facts — don't recompute"), Quant Fingerprint card in the Financials cluster. Altman/Beneish n/a-gated for Financial Services. Spec: `docs/superpowers/specs/2026-06-10-quant-fingerprint-design.md`.

- **8-K + Form 4 monitoring (investor-portal sub-project 4, 2026-06-10)** — daily 06:30 UTC scan of the universe (theme seeds ∪ active theses): 8-Ks item-code-prefiltered (skip ⊆ {7.01, 9.01}; 2.02 kept — guidance lives there) and Haiku-classified into `material_events` (badge + `MaterialEventsDrawer` on `/status`, high-materiality amber rows on Today, `?expand_events` deep link); Form 4 via FMP `insider-trading/search` into `insider_transactions` (sha256 natural key over parsed values — idempotent; `url`/accession kept for EDGAR backfill; wire keys live-verified) → 90-day aggregate (`insider_signal.py`, pure) → `signals` rows `signal_type="insider"` per (ticker, theme) with `signal_history` dual-write → bounded modifier on the discovery combined score (+5 cluster / +2 net buying / −3 pronounced selling, 48h staleness, clamp [0,100]) + insider chip on discovery cards. New `/api/events` router (list/dismiss/scan); board entries carry a 14-day events summary. 8-K-scan failures are fault-isolated from the insider ingest. ~45 new backend tests (suite 432 green); frontend lint + build + node tests clean. Spec: `docs/superpowers/specs/2026-06-10-material-events-design.md`.

- **Today dashboard (investor-portal sub-project 3)** — `/` is now a morning briefing (severity banner → today+3d calendar lanes → needs-attention list with workspace kick-off); themes grid moved to `/themes`. Frontend-only composition of board/calendar/questions endpoints, 60s visibility-gated polling, per-section degradation (error notes never blank surviving rows). `WorkspaceButton` extracted to `components/status/`. Spec: `docs/superpowers/specs/2026-06-10-today-dashboard-design.md`.

- **Unified calendar (investor-portal sub-project 2)** — `/catalysts` Calendar view: US high-impact econ + universe earnings + thesis catalysts merged by stateless `calendar_events` service; week lanes + agenda UI; EarningsDrawer deep link. Spec: `docs/superpowers/specs/2026-06-09-unified-calendar-design.md`.

- **Investor-portal quick-fixes pack (2026-06-09)**. The four follow-ups from the peer-comparison handoff (`docs/superpowers/2026-06-09-investor-portal-handoff.md`): (1) deep-dive valuation ratios fixed — `node_deep_dive` now also fetches `ratios-ttm` and both `_fmt_fundamentals` + `_build_curated_financials` resolve P/E, EV/EBITDA, P/B, P/FCF, P/S, PEG, dividend yield, and interest coverage via `_first_metric` fallback chains (live /stable/ names first, legacy `key-metrics-ttm` spellings kept for old persisted payloads); ROE/ROIC/ROA read the live key-metrics names with true-ROA preference, matching `peer_comp`. Live-verified against NVDA: 8 of 11 fields None before, all populated after. (2) `fcf_margin` derived as `p_s ÷ p_fcf` in `peer_comp._fetch_one` (wire-served value still wins if FMP ever adds one) — the column populates everywhere. (3) `DifferentiationCard` migrated to the shared `components/peers/PeerCompTable` (proper pct/multiple/money formatting replaces the raw-ratio bespoke table). (4) Peers-tab stale-comp race closed with a fetch-sequence counter (mount fetch can no longer overwrite a fresher post-edit fetch). 6 new backend tests (suite 342 green); frontend lint + build clean.
- **Peer comparison surface (2026-06-09)**. First sub-project of the investor-portal track (spec: `docs/superpowers/specs/2026-06-09-peer-comparison-design.md`, local-only). `peer_comp.py` promoted from workspace-internal to the single shared table builder: schemas moved to `models/peer_comp.py` (re-exported from `workspace_schemas` — old persisted `step_outputs` still validate, pinned by test), `METRIC_FIELDS` widened to 16 (valuation + growth + margins + returns + market cap) across 4 FMP endpoints with fallback wire-keys (`_first` helper). Live verification corrected two production assumptions: /stable/ valuation multiples live on `ratios-ttm` (not `key-metrics-ttm`), and true ROA is `returnOnAssetsTTM` (the deep-dive's `returnOnTangibleAssetsTTM` is the tangible variant). New `peer_sets` table (migration `d9659a472017`) + `services/peer_sets.py` (seed = resolved `competitor_landscape` ∪ new `FMPClient.get_stock_peers`, cap 8; manual edits cap 12; commit-free service — callers own the session); workspace `_fetch_resolved_peers` now delegates to `peers_for_ticker` (curated-first, landscape fallback). New `/api/peers` router (set GET/PUT with route-owned commits + IntegrityError-tolerant concurrent-seed, `/{ticker}/comp`, ad-hoc `/compare?tickers=` — declared before `/{ticker}` because "compare" parses as a ticker; pinned by test). Frontend: Peers tab on `/company/[ticker]` (chip editor, optimistic update with PUT-only rollback, recoverable comp-error state) + standalone `/compare` (URL-state, shareable) sharing `components/peers/PeerCompTable` (grouped headers, best-in-class tint via direction map, median + Δ footers). 39 new backend tests; full suite green; frontend lint + build clean; Playwright E2E smoke verified (auto-seed, chip edits, deep link, focus switch). Follow-up worth noting: `graph/nodes.py` + `CuratedFinancials` still read valuation multiples from `key-metrics-ttm` wire names that the live API no longer serves — deep-dive valuation ratios are likely silently None; not fixed in this pack.

- **Workspace robustness pack (2026-05-27)**. Five-issue cluster from the in-conversation audit: (1) typed preflight DTO + `GET /api/workspace/{ticker}/preflight` endpoint backed by a shared `_gather_preflight_facts` helper that the existing raising `_preflight` now delegates to; (2) per-ticker `asyncio.Lock` registry (`_acquire_ticker_lock`) closes the `kick_off`-to-claim race where two parallel calls could both pass the draft-absent check before either `WorkspaceRun` INSERT landed; (3) `workspace_runs.step_outputs` validated against per-step Pydantic schemas (`_STEP_SCHEMAS` + `_validate_step_outputs`) before persist — schema mismatches become `{"error": "schema_validation_failed: ..."}` and force `partial` status; existing `{"error": ...}` entries pass through; unknown step names pass through to avoid gating schema evolution; (4) `transcript_delta.compute_delta` in-flight guard via module-level `_IN_FLIGHT: dict[(ticker, fingerprint), asyncio.Event]` deduping Haiku calls within the process (leader/follower pattern; best-effort — a cross-session follower can miss the leader's row until its caller COMMITs), backed by a SAVEPOINT-guarded INSERT that returns the cross-session winner's row on a lost unique-constraint race instead of raising, plus a defensive `IntegrityError` catch in `workspace_steps.step_research` so any stray race no longer aborts a 30-40s workspace run; (5) frontend `useWorkspacePreflight` hook (with a `MISSING_COPY` map exhaustively keyed by the union) wired into the kick-off buttons on `/status` (extracted into a `WorkspaceButton` child to satisfy rules-of-hooks inside `.map()`) and the deep-dive `ReportHeader` badge — disabled state + structured tooltip; when `in_flight_run_id` is set, the button stays enabled and routes to the running report instead of attempting a duplicate kick-off; (6) sticky amber unsaved-draft banner on `/model/[ticker]` that lifts `handleSave`/`handleDiscard` to `ModelWorkspace` so banner + footer share single-source handlers (fixed a latent `setBusy(false)` leak on rejected saves in the process). 11 new tests (4 preflight + 2 race + 4 step_outputs + 1 transcript_delta race); backend suite 297/297 green; frontend lint + build clean. Plan at `docs/superpowers/plans/2026-05-27-workspace-robustness-pack.md` (local-only).

- **Earnings call transcript delta analysis.** Detects QoQ language shifts across the 4 most recent transcripts on the 9 deep-dive category axes; null axes when management doesn't materially address them. New `transcript_deltas` table keyed by `(ticker, sha1 of (year,quarter) tuples)` — cache hit on identical inputs is free; new transcript drops trigger a new row (history capped at 8 per ticker). Three endpoints under `/api/transcripts/delta/{ticker}` (POST compute, GET latest, GET history). Surfaces as a `WhatChangedPanel` immediately above Management & Governance on the deep-dive page; workspace Step 2 consumes it best-effort as a prompt input. ~11 new tests; full backend suite green.

- **Verdict outcome tracking (alpha feedback loop)**. New `/performance` page (8th nav link) answers four questions in one view: do my verdicts beat SPY, which verdict bands predict, which themes are the alpha source, which signals drive winners. Every research-run + workspace-run terminal verdict now writes a row to `verdict_outcomes` with entry prices anchored to the first trading day strictly after emission (no look-ahead bias) for the ticker + SPY + sector ETF (via static `sector_etf_mapping` table seeded with 11 SPDR sectors) + an equal-weighted basket of `theme.seed_tickers` (constituents snapshotted at emission so theme membership drift doesn't poison comparisons). Plus a `signal_snapshot` JSONB capturing the signals row + run-internal scores (deep-dive category scores or workspace step verdicts + kill-criterion state + model assumptions) for quartile-bucket attribution. Daily APScheduler cron at 03:00 UTC fills due offsets (+1d / +1w / +1m / +3m / +6m); outcome closes when 6m lands. Supersede stamping: a new same-source-type verdict for the same `(ticker, theme_id)` flips `superseded_at` + `realized_*_pct` on the prior open outcome (cross-source and cross-theme verdicts stay independent). 4 endpoints under `/api/outcomes`: `POST /backfill` (idempotent one-shot), `GET /by-source/{type}/{id}`, `GET /` (paginated with theme/verdict/source/superseded/closed filters), `GET /summary` (overall + by_verdict + by_theme + by_signal_bucket quartile rollups). Spec uses FMP `historical-price-eod/dividend-adjusted` for all benchmarks — splits + dividends matter for accurate return measurement (added `FMPClient.get_historical_price_adjusted` for this). Best-effort hooks in `PipelineService._record_terminal_outcome` and `WorkspaceService._record_workspace_outcome` use dedicated `unit_of_work()` sessions so outcome failures never crash the run that succeeded. `backfill_from_history` walks completed research_runs (statuses: completed | watchlist | pass — yes, all three including negative-verdict control group) and terminal workspace_runs with verdicts; safe to re-run. CLI at `python -m backend.scripts.backfill_outcomes`. Migration `12f874fd1d02` adds 3 tables + 4 indexes (two of them partial: `closed_at IS NULL`, `superseded_at IS NULL`). 33 new tests (24 outcome_tracker + 9 outcomes_api); full backend suite 221/221 green; frontend build clean. 7 React components under `frontend/components/performance/` (PerformanceFilters with URL-state filtering, HeroBand, ByVerdictTable, ByThemeTable with click-to-filter rows, BySignalBucketPanel with signal switcher, OutcomeList linking back to source run, ReturnCell shared formatter). Spec at `docs/superpowers/specs/2026-05-10-verdict-outcome-tracking-design.md`; plan at `docs/superpowers/plans/2026-05-10-verdict-outcome-tracking.md` (both `.gitignore`d, local-only). Followup work explicitly out of scope: position sizing / Sharpe / drawdown stats, within-outcome sparkline, status-board excess-return badge, multi-leg tracking.

- **Theme membership editing (atomic add/remove)**. Themes can now have tickers added/removed inline from the theme detail page. Backend gained `_normalize_tickers` helper (uppercase, strip, dedupe order-preserving, tolerates legacy list-of-dicts shape) applied on POST/PUT plus two new atomic sub-routes — `POST /api/themes/{id}/tickers` (idempotent on duplicate, 400 on empty) and `DELETE /api/themes/{id}/tickers/{ticker}` (idempotent on absent) — both returning the updated `ThemeRead`. Removing a ticker preserves its `signals` and `signal_history` rows by design; the scheduler's union-with-previously-signalled rule keeps refreshing them. Frontend `ThemeDetailClient.tsx` got a collapsible `TickerEditor` (chip list with X buttons + comma/space-separated add input) wired to the new `themesApi.addTicker` / `removeTicker` methods in `lib/api.ts`; mutations call `router.refresh()` so the server component re-fetches the theme + discovery results and re-flows `is_seed` flags. CONTEXT.md gained a Theme membership glossary entry. Backend tests in `test_themes_api.py` (14 tests, all green) pin normalization, idempotency, 400/404 paths. End-to-end smoke verified via Playwright. **Deferred:** delete-theme UI on the index page — would need backend FK cascade work first (`signals` / `signal_history` / `surprise_alerts` / `watchlist` / `research_runs` all reference `themes.id` without `ondelete="CASCADE"`, so the existing `DELETE /api/themes/{id}` fails on any non-empty theme).

- **Signal velocity history**. New sibling `signal_history` table appended on every scheduler write so multi-month sparklines and surprise-threshold tuning have a real time series. Existing `signals` table semantics unchanged (still 1 row per `(ticker, theme_id, signal_type)`, last-write-wins) — discovery engine read path, surprise-alert prior-ratio lookup, pipeline reads all untouched. New `services/signal_history.list_signal_history()` helper + `GET /api/themes/{id}/signals/{ticker}/history?signal_type=velocity&days=N` endpoint expose the time series; clamped to [1, 365] days. Frontend typed client (`getSignalHistory` in `lib/api.ts`) added; sparkline UI deferred (YAGNI). Migration `8b4fd10f00d3` adds the table + composite index `(ticker, theme_id, signal_type, computed_at)` matching the time-series read pattern.

- **Tier 3.9 — Workspace 5-step loop**. New `/workspace/[runId]` page with SSE-streamed 5-step refresh: Update/Refresh (model diff + version bump), Research (Haiku triage), Validation (reverse-DCF re-run), Challenge (stress-test with kill-criterion writeback to /status), Differentiation (peer-comp via competitor_landscape + read-throughs). New `workspace_runs` table; status board staleness now considers latest workspace run. Manual per-ticker trigger from /status row, /pipeline/[runId] header, and /workspace index page (top-nav). All 5 step types render in collapsible cards; verdict badge mirrors status-board palette. Catalyst writebacks deferred to v1.5 (Catalyst.status column not yet present). 17 commits on the branch; full backend suite 82/82 green; frontend build green. Spec at `docs/superpowers/specs/2026-05-09-tier-3-9-workspace-loop-design.md`; plan at `docs/superpowers/plans/2026-05-09-tier-3-9-workspace-loop.md`.

- **`ConcentrationRiskPercentage1` dropped from XBRL ingest + routing**. Investigation 2026-05-09 confirmed the SEC `companyfacts` endpoint does not expose this concept at all (zero entries across NVDA/AAPL/TSM/AMD/BWXT live checks; zero rows in our DB across 238+ ingested filings) — it's always disclosed dimensionally and the API only returns un-dimensioned parent facts. Removed from `CONCEPT_WHITELIST` in `clients/edgar.py`, removed from `EDGAR_ROUTING` (Risk Assessment, Business Quality) in `graph/deep_dive_routing.py`, and removed from `node_deep_dive`'s docstring + CLAUDE.md prose. Concentration intel still flows in via the existing Phase B narrative extraction path (`Relationship.unnamed=true` rows surfaced through `RELATIONSHIP_ROUTING`). Two new pinning tests in `test_deep_dive_routing.py` prevent the concept from being silently re-added to either the whitelist or the routing tables. Note retained inline in `clients/edgar.py` and `deep_dive_routing.py` explaining why the slot is empty so future me doesn't undo this. To recover dimensional concentration data we'd need to parse XBRL instance documents directly — left as a future feature, not polish.

- **`FanoutService._statuses` bounded with insertion-order eviction**. The in-memory status dict is now an `OrderedDict` capped at `DEFAULT_MAX_STATUSES=128`; `_record()` evicts the oldest entry on overflow. Terminal entries (completed/failed) are by construction the oldest in any normal flow, so eviction targets them first — a still-running fanout would only be evicted after 128 concurrent runs, which can't happen in practice. Cap is constructor-injectable (`max_statuses=`) for tests. New `backend/tests/test_fanout_eviction.py` (4 tests) pins under-cap retention, single-overflow eviction, repeated-overflow trim, and a sanity check on the production default.

- **Deep-dive routing + helpers lifted out of `node_deep_dive`** (partial pass at #4 + #5 of the architecture audit). The five per-category routing tables (`TRANSCRIPT_ROUTING`, `MACRO_ROUTING`, `EDGAR_ROUTING`, `FILING_EXCERPT_ROUTING`, `RELATIONSHIP_ROUTING`) plus `FILING_EXCERPT_BUDGET_CHARS` moved from `graph/nodes.py` (lines 50-130) to a new `backend/app/graph/deep_dive_routing.py`. Re-exported from `nodes.py` so the existing context-builder closures continue to reference them by name unchanged. Added a `CategoryRouting` dataclass + `routing_for(category)` accessor that exposes all 5 sources for a category in one place — the new single source of truth for "what data does this deep-dive category see?". Two pure helpers extracted to `backend/app/graph/deep_dive_helpers.py`: `unwrap_gather_result(result, default)` (was `_unwrap`, the asyncio.gather error tolerator) and `format_fact_value(value, unit)` (was `_fmt_fact_value`, the XBRL value formatter). Both were nested defs inside `node_deep_dive` only reachable via running the whole node; now module-level and unit-testable. Tests pin per-category routing payloads + invariant checks (`test_deep_dive_routing.py`, 8 tests) and helper behavior across USD/pure/other units + exception unwrapping (`test_deep_dive_helpers.py`, 8 tests). 49/49 backend tests pass. **Deferred to a fresh-eyes session:** the full `DeepDiveContext` extraction that would lift the 7 stateful context-builder closures (`_build_transcript_context`, `_build_macro_context`, `_build_technical_context`, `_build_sentiment_context`, `_build_edgar_context`, `_build_filing_excerpt_context`, `_build_counterparty_context`) up to module level — they bind 10+ outer-scope vars (`fmp_data`, `state.curated_financials`, `edgar_facts`, `filing_sections`, `counterparty_context`, `signals`, etc.) so doing it well requires the dataclass scaffold, ~3-4 hours of careful work, and verification against a real run. Not done at midnight against the central LangGraph node. **#2 (Citation contract drift) skipped:** the architecture audit overstated the friction — FMP/EDGAR/X clients are uniformly `tuple[T, Citation]`, only `FRED.get_all_macro()` returns `list[Citation]` (intentional, one per series, single caller correctly iterates). A `CitedResult[T]` wrapper across all clients would touch ~80 call sites for marginal benefit; not worth it.

- **`unit_of_work()` context manager for write paths**. New helper in `backend/app/db.py` opens a session, commits on clean exit, rolls back on exception, propagates. Replaces the `async with async_session() as db: ... await db.commit()` ceremony at sites where forgetting the commit would silently drop the work. Migrated 6 sites: 4 in `fanout.py` (the 4-stage pipeline — ingest / extract / extract_transcripts / resolve), 1 in `earnings_scheduler.py` (the per-ticker daily refresh loop), and 1 in `graph/nodes.py` (catalyst promotion inside thesis). Read-only paths (the `_fetch_*` helpers in `pipeline.py` and `nodes.py`) keep using `async_session()` directly — no commit machinery is needed without writes. Two write sites deliberately left as-is: `model_baseline.py:246` (uses `db.refresh(row)` after explicit commit to pick up DB-generated defaults — works fine, the auto-commit-on-exit becomes a no-op) and `pipeline.py:325` (bespoke try/commit/rollback already correctly structured for its read-after-write flow). New `backend/tests/test_unit_of_work.py` (4 tests, mocked session) covers commit-on-clean, rollback-on-exception, double-commit safety, and rollback-after-explicit-commit.

- **Ticker value object at the API boundary**. New `backend/app/models/ticker.py` defines `Ticker = NewType("Ticker", str)` plus `normalize_ticker(value)` (validates against `^[A-Z][A-Z0-9.\-]{0,9}$`, strips whitespace, uppercases) and a `TickerPath` FastAPI dependency that normalizes-or-400s. Migrated 21 routes across `backend/app/api/{models_api,filings,earnings,fanouts}.py` from `ticker: str` + manual `ticker = ticker.upper()` at every entry to `ticker: Ticker = Depends(TickerPath)`. The 12-line scattered normalization is now a single seam — bad input is rejected at the boundary with a descriptive 400 instead of producing wrong-row lookups against an uppercase-stored DB. `Ticker` is a `NewType[str]` (runtime-identical to `str`), so ORM queries and downstream services that take `str` continue to work without changes; no service-layer churn. Defensive `.upper()` calls inside service helpers and on values from non-Ticker sources (LLM output, ORM round-trips, JSONB dicts) are left in place — they're correctness for inputs the boundary can't normalize. New `backend/tests/test_ticker.py` (12 tests) pins normalization, validation, and 400 behavior. Verified end-to-end with FastAPI TestClient: lowercase/mixed/dotted symbols normalize correctly; whitespace and bad characters produce 400.

- **Doc + memory hygiene pass**. CLAUDE.md fixed two stale notes — backend test framework (`backend/tests/` exists; runs via stdlib `unittest`) and the model `cell_path` doc now mentions the typed module backing validation. README.md fixed a real contradiction with CLAUDE.md — the prose claimed "three interrupt gates" and the Pipeline diagram showed three ⚡ INTERRUPTs, but phases 1-5 actually run continuously and only position_monitor (phase 6) is manually gated. Diagram renumbered correctly (phase 1 = quick_screen, 2 = deep_dive, etc.) and Haiku/Sonnet annotations moved inline. Added Status Board to the "core workflows" list (was undersold as "four workflows" — there are five major top-level pages now). Memory cleanup: deleted `project_deep_dive_next_iteration.md` (a month-old backlog referencing the long-deleted `BACKLOG.md`); rewrote `reference_framework_roadmap.md` to reflect that Tiers 1.1/1.2/1.3/1.4/2.5/2.6/3.7/3.8 have all shipped (only Tier 3.9 workspace loop status unclear); refreshed the MEMORY.md index hook accordingly.

- **Cell-path deepening (typed value object across the wire)**. The model's `cell_path` wire format (`drivers.<period>.<key>` / `<stmt>.<line>.<period>` / `assumptions.<key>`) was previously a stringly-typed protocol — backend split + len-checked in `_apply_edit`, frontend constructed inline in `ForecastGrid`/`DriverPanel`, format documented only in CLAUDE.md. Deepened: new `backend/app/models/cell_path.py` with a `CellPath` discriminated union (`DriverPath` | `StatementCellPath` | `AssumptionPath`), `parse()` / `to_str()`, registry-backed validation against `DRIVER_KEYS` + per-statement line-item registries + period regex. `models_api.py::_apply_edit` now dispatches on the typed value; the dead `isinstance(cur, dict)` defensive branch is gone. `model_state.py` helper functions delegate to the typed module via deferred import. New `frontend/lib/cellPath.ts` mirrors the types and exposes `driverPath` / `statementPath` / `assumptionPath` builders + `toWire()`; `DriverPanel` and `ForecastGrid` use the builders instead of inline template literals. `AssumptionPath` is intentionally restricted to ModelCell-shaped keys (`discount_rate`, `terminal_multiple`, `perpetuity_growth`, `tax_rate`); the categorical assumptions (`terminal_method`, `plug_priority`) are deferred to a future typed `PATCH /draft/assumptions` endpoint — not built yet because no caller wants it. `CellRenderer` keeps its `cellPath: string` prop (downstream churn not worth it; the seam now lives at the builder layer). New tests: `backend/tests/test_cell_path.py` (12 tests — wire format, parse, round-trip, rejection of unknown shapes/keys/periods) + `frontend/lib/cellPath.test.mts` (3 wire-format tests). All pass; ESLint clean. The remaining frontend registry duplication (DriverPanel's `GROUPS`, ForecastGrid's line-item arrays mirror backend `DRIVER_KEYS`/`LINE_ITEMS_*`) is a separate friction — out of scope for this pass.

- **Model page theme migration (light theme)**. All 9 components under `frontend/components/model/` (`CellRenderer`, `DriverPanel`, `ForecastGrid`, `FormulaBar`, `HistoryDiffViewer`, `ReverseDcfPanel`, `SensitivityHeatmap`, `ThesisVsPricedTable`, `WhatIfScratchPanel`) plus `frontend/app/model/[ticker]/page.tsx` migrated from hardcoded dark Tailwind classes (`bg-slate-*`, `text-slate-*`) to light-theme CSS variable tokens (`var(--surface)`, `var(--text)`, `var(--text-muted)`, `var(--border)`, `var(--primary)`). Sticky table headers + sticky left line-item column now use `bg-[var(--surface)]` so values stay legible on the light app shell. `SensitivityHeatmap` keeps inline RGB styles for the diverging palette (intentional exception — value-relative coloring). `ThesisVsPricedTable` gained semantic delta color coding. ESLint clean post-migration; zero remaining hardcoded dark-theme color classes in the model page tree. Triggered by table values being invisible against the light app background unless highlighted. Also captured the Matt Pocock skill adapter setup in `CLAUDE.md` (`docs/agents/issue-tracker.md` + `triage-labels.md` + `domain.md`).

- **Deep-dive transcript fetch hits correct ticker**. `backend/app/graph/nodes.py` was calling `fmp.get_earnings_transcript(state.ticker)` with no `year`/`quarter`, routing to FMP's `earning-call-transcript-latest` endpoint (which ignores the `symbol` param) — deep-dive transcript analysis was receiving arbitrary tickers' transcripts. Fix: promoted `_fetch_recent_transcripts` → public `fetch_recent_transcripts` in `services/edgar_transcripts_relationships.py` and extended its return shape to `(list[dict], Citation | None)` so it can plug into the deep-dive `asyncio.gather` in place of the broken call. The helper walks back from the current quarter calling the explicit `(year, quarter)` endpoint, caps lookback at 8 quarters, and stamps requested `year`/`quarter` onto each entry. Existing relationship-extraction caller in the same module updated to discard the new citation. Citation-add in `node_deep_dive` guarded against `None` (no citation when zero quarters returned data).

- **Deep-dive polish (PR #25)**. Merged to `main` 2026-05-07 as `16839f4`. Fix-pack across 8 files (+112/-68): (1) extracted shared `renderMarkdown.tsx` utility under `frontend/components/deep-dive/` so `AICompanionPanel` and `EarningsDrawer` render thesis/risk/summary markdown consistently instead of each reinventing the conversion; (2) chart-label hygiene in `RPOTrend.tsx`, `GrowthEarnings.tsx`, and the run page; (3) thesis fallback handling in `backend/app/graph/nodes.py` + `backend/app/models/phase_schemas.py` so a missing thesis doesn't blank the report; (4) RPO trend chart now hides itself when the latest XBRL fact is older than 2 years (avoids the "looks current but is actually stale" failure mode on tickers whose 10-K filings haven't been re-ingested).
- **Tier 3.7 + 3.8 — Editable financial model + reverse DCF (PR #24)**. Merged to `main` 2026-05-06. New `/model/{ticker}` page with three hash-routed tabs (`#forecast` / `#reverse-dcf` / `#history`). `ModelState` Pydantic in `backend/app/models/model_state.py` carries 8 historical Q + 8 forecast Q + 5 forecast Y, drivers per period, three statements keyed `[line_item][period] -> ModelCell`, and assumptions (WACC, terminal growth, terminal multiple, share counts). Pipeline: `model_baseline.build_baseline_state` (Sonnet baseline-drivers via `graph/model_baseline_node.py` — markdown-fence stripped, `max_tokens=8192`, annual cloned to quarterly to fit token budget) → `model_balancing.recompute` (P&L → CF → BS rollforward, plug into `retained_earnings`; cell-level overrides preserved through downstream calcs) → persisted to `ticker_models` (versioned) or `ticker_model_drafts` (one row per ticker). Reverse DCF in `services/reverse_dcf.py`: bisection solvers (`solve_implied_driver`, `solve_implied_irr`), 21×21 sensitivity grids via `_apply_uniform_override` + `deepcopy(state)` + `recompute()` (NOT the unused `overrides=` parameter on `dcf()`), and `thesis_vs_priced_in`. API surface at `/api/models/{ticker}/...` (initialize / draft PUT / save / discard / reverse-dcf / versions / diff). `StateCitation` gained `cell_path` for cell-level deep-linking (migration `2db2e8812418`). Frontend components in `frontend/components/model/`: `ForecastGrid`, `DriverPanel`, `FormulaBar`, `CellRenderer`, `ReverseDcfPanel`, `SensitivityHeatmap`, `ThesisVsPricedTable`, `WhatIfScratchPanel`, `HistoryDiffViewer`. Deep-dive `ReportHeader` got a Model status badge (links to `/model/{ticker}`). Seven smoke scripts cover state, balancing, baseline, DCF, reverse-DCF, diff, models API, plus an E2E init→edit→save→reverse-dcf cycle. Code-review fix pass (PR #24 follow-up `2ac7847`): reuse shared `app.state.fmp` singleton in `_fetch_live_price` (no per-request `httpx.AsyncClient` leak); map `_apply_edit ValueError → 422` instead of bare 500; normalize `ticker.upper()` in all model endpoints (matches filings/earnings/pipeline convention); drop `getReverseDcf` call from `ModelStatusBadge` so report-page opens don't fan out FMP `/quote` calls; add `loaded` sentinel to badge so the "Create model →" link doesn't flicker for tickers with a saved model.
- **Tier 1.4 — Read-through engine on supply-chain graph (PR #21, stacked with Tier 2.6)**. Surfaces peer events (earnings catalysts from Tier 1.3 + recently completed runs) against active status-board theses by joining the existing `relationships` + `competitor_landscape` graph in either direction. New `read_through_dismissals` table (UUID PK + FK→`research_runs` ON DELETE CASCADE + unique on `(run_id, event_key)` so deterministic keys like `earnings:NVDA:2026-08-15` and `run_complete:<uuid>` survive recomputation). Three service layers in `services/read_through.py`: `compute_peer_events` (catalysts ∪ completed runs in window), `resolve_read_throughs` (joins peer events × thesis tickers via two batched queries — `relationships` non-competitor + `competitor_landscape` JSONB walk — buckets by `(run_id, event_key)`, dedupes links by `(type, direction)`, sorts by signal-strength rank: customer/supplier/partner/joint_venture > competitor > licensor/licensee/distributor/reseller > other, then by `event_date desc`), and `summarize_read_through` (lazy Haiku one-paragraph impact summary that re-resolves the event from its key, pulls `CounterpartyContext` for the thesis ticker, and renders both into the prompt). API at `/api/status/read-throughs` — `GET` (default window now ±30d so upcoming earnings appear), `POST /dismiss` (idempotent via `pg_insert(...).on_conflict_do_nothing(constraint=...)`), `POST /summary` (400 on ValueError, 502 + `logger.exception` on others). Frontend: `RelationshipLink` / `ReadThroughItem` / `ReadThroughsByRun` types + `readThroughs` client in `lib/api.ts`; new `components/status/ReadThroughDrawer.tsx` (4 UI states — empty / row / dismissing / summary); `/status` page polls read-throughs on the same 60s cadence as the board, renders an amber `⟿ N` badge per row when items exist, click expands an inline drawer beneath the row with relationship-type chips (verbatim quote in tooltip), Dismiss + "Generate impact summary" buttons. Smoke script at `backend/scripts/smoke_read_through.py` exercises the full loop (seeds synthetic earnings catalyst → asserts indexer surfaces it → asserts resolver matches it via existing relationship edges → asserts dismissal filters it out → cleans up) and passes 3/3 against current DB state. Health-flip events deferred to v1.5 (status board doesn't persist transitions; needs `status_board_snapshots` table). Also caught a code-review-driven cleanup pass: extracted `_extract_thesis_summary` helper (was duplicated 3x), tightened raw `event.payload` dict-repr interpolation in the prompt to `description or thesis_summary`, bounded the dismissal query with both `run_id IN` and `event_key IN` predicates so it stays well-indexed, anchored the smoke script's synthetic catalyst FK to `target.run_id` rather than a string-`None` fallback. Spec at `docs/superpowers/specs/2026-05-04-tier-1-4-read-through-engine-design.md`; plan at `docs/superpowers/plans/2026-05-04-tier-1-4-read-through-engine.md` (both `.gitignore`d, local-only).
- **Tier 2.6 — Live thesis status board (PR #21, stacked with Tier 1.4)**. New `/status` top-level page aggregates the latest completed run per `(ticker, theme)` into a compact row table with health badges (Healthy / Imminent / Stale / Triggered / Broken), nearest-catalyst proximity, kill-criteria summary, theme/health filter chips, and an archive escape hatch. Read-time aggregation in `services/status_board.py` (no caching, no scheduler) joins `research_runs` (DISTINCT ON `(ticker, theme_id)`) with the catalysts and `kill_criterion_states` tables. Health resolution is first-match-wins (broken > triggered > stale > imminent > healthy) with reasons accumulating every contributing condition. Stale = no re-run in 90d; Imminent = catalyst within 30d OR window currently in progress. New ORM `KillCriterionState` (per-run, ordinal-keyed; default `armed` is implicit absence) + `archived_at` nullable timestamptz on `research_runs`, both in alembic revision `0b7ff9421fa5` (hand-written downgrade fully reverses, fixing the pattern from the catalysts migration). Five new endpoints in `api/status.py`: `GET /api/status/board?theme_id=&include_archived=`, `POST /api/runs/{id}/archive` + `unarchive`, `GET /api/runs/{id}/kill-criteria`, `PUT /api/runs/{id}/kill-criteria/{ordinal}` (idempotent upsert). Report endpoint extended with `kill_criterion_states[]` so the report-page kill-criteria section can hydrate without a parallel fetch. Frontend `app/status/page.tsx` polls every 60s while `document.visibilityState === "visible"`, pauses on hidden; row click → `/pipeline/[runId]`; ⋯ menu archives. Kill-criterion toggle inline on `ThesisCard.tsx` → `Armed`/`Triggered` pill click expands an editor with optional note → `PUT` returns the persisted state and the local copy merges. End-to-end Playwright walkthrough verified all five health states render correctly across 8 active theses. Caught a FastAPI 0.115 / Python 3.12 footgun: `from __future__ import annotations` causes `-> None` returns on `status_code=204` endpoints to evaluate as the string `"None"` and trip an internal assertion — `api/status.py` and `api/read_through.py` deliberately omit the future import. Spec at `docs/superpowers/specs/2026-05-04-tier-2-6-status-board-design.md`; plan at `docs/superpowers/plans/2026-05-04-tier-2-6-status-board.md`.
- **Tier 1.3 — Catalyst calendar + signposts as first-class object (PR #20)**. Squash-merged to `main` 2026-05-05 as `172e6d7`. New `Catalyst` ORM (one row per catalyst per run, with `expected_window_start/end` + `expected_date` + `date_source`), Sonnet-emitted enriched catalyst schema (timeframe / description / type / signposts / linked_pillar), FMP earnings-calendar override, and a top-level `/catalysts` page with proximity buckets (≤30d / 30-90d / >90d / undated). Per-ticker calendar inside `/pipeline/[runId]`. Promotion service writes catalyst rows after every successful thesis pass; idempotent on (run_id, ordinal). Includes a follow-up fix from the codex worktree branch that scoped the run-page calendar to the current run only.
- **Transcript relationship extraction (Phase B-T)**. Last 4 quarters of earnings call transcripts run through the existing Haiku relationship extractor. Rows persist into `relationships` with `source_type='transcript'`, `filing_id=NULL`, `transcript_year/quarter` populated; idempotency tombstoned in new `transcript_extractions` table. New endpoint `POST /api/transcripts/extract-relationships/{ticker}`. `FanoutService` grew a 4th stage (`extract_transcripts`) between extract and resolve. Prompt edits in `edgar_relationships.py`: opening line broadened to "filings or earnings call transcripts" and one new rule to skip Q&A analysts. Resolver and supply-chain graph pick up transcript rows automatically via the denormalized `Relationship.ticker` column. Verified on BWXT: 4 transcripts extracted (2025 Q1–Q4), 23 transcript-sourced relationships persisted, 22 of which surface counterparties NOT present in any BWXT filing — Kairos Power, TerraPower, Ontario Power Generation, Bruce Power, Rolls-Royce, GE Hitachi etc. Migration replaces `uq_relationships_filing_section_counterparty_type` with two partial unique indexes (`uq_relationships_filing` WHERE filing_id IS NOT NULL; `uq_relationships_transcript` WHERE filing_id IS NULL). FMP coverage caveat documented in extractor: the `earning-call-transcript-latest` endpoint ignores the symbol param, so the new `_fetch_recent_transcripts` walks back from the current quarter calling the explicit `(year, quarter)` endpoint and stamps the requested year/quarter onto each entry (the API does not echo `quarter` reliably).
- **Stale `competitor` references cleanup (post-PR #17)**. Dropped `"competitor"` from `_build_counterparty_context` `type_order` and the prompt anchor instruction in `backend/app/graph/nodes.py`; updated the `Relationship.relationship_type` doc comment in `backend/app/models/filing.py` to reflect the removed type and point readers at `competitor_landscape` for the new home. The `_normalize_relationship` ALLOWED_TYPES gate would have dropped any LLM "competitor" output regardless, so output was already harmless — this just removes the stale prompt promise and doc drift.
- **Competition section sourced from 10-K Item 1 (PR #17)**. New `edgar_competition.py` Haiku extractor with Pydantic structured output produces segments → areas of competition → named competitors; persisted via two new ORM models (`FilingSegment`, `CompetitorLandscape`) and a JSONB `competitors[]` column. Idempotent on `filing_sections.competition_extracted_at` (only stamped on success — transient Haiku errors don't tombstone). Endpoints: `POST /api/filings/extract-competition/{ticker}`, `GET /api/competition/{ticker}`. Resolution extended via `resolve_competition_for_ticker` (alias reuse → exact → fuzzy ≥95) writing back into the JSONB array with `flag_modified`; new aliases also write through to existing `Relationship` rows. `POST /api/relationships/resolve/{ticker}` now returns `{relationships, competition}` (typed as `CombinedResolveSummary` in `frontend/lib/api.ts`). Frontend: new `Competition.tsx` between Business Quality and Supply Chain, segments→areas→competitor chips with tracked-ticker click-through. `competitor` type removed from relationship extraction, supply-chain reciprocity, and supply-chain UI; one-time DELETE of legacy competitor rows in migration `6e374957cafa`. Verified on BWXT: 2 segments, 3 areas, 18 competitors, 8 auto-resolved tickers.
- **Theme-level fan-out card refresh polish**. `TickerFilingsCardHandle` now exposes `load()` alongside `ingest()`, and `ThemeFilingsPanel` expands before fan-out and refreshes all mounted ticker cards once polling reaches a terminal status, so cards no longer stay on stale "no sections yet" data after `Fan out N` completes.
- **LLM prompt behaviour validation after PR #16 merge**. Merged PR #16 branch into `main`, ran a fresh ORCL deep-dive, and inspected Business Quality / Risk Assessment / Future Durability. Initial run still used plain counterparty names for resolved entities (e.g. AWS/Azure); updated `_build_counterparty_context` to render resolved counterparties as `$TICKER — name — relationship_type` and strengthened the instruction to avoid product/vendor-name-only references. Follow-up ORCL run rendered `$AMZN AWS` and `$MSFT Azure`; Google Cloud remained plain text because its relationship row is unresolved.
- **Tier 3 v2 completion — fan-out orchestration + relationship prompt routing + Item 1A regex (PR #16)**. New `FanoutService` with in-memory status tracker wired through `app.state.fanout`. Three endpoints: `POST /api/themes/{id}/relationships/fanout` + `POST /api/tickers/{ticker}/relationships/fanout` (both 202 with `fanout_id`), `GET /api/fanouts/{id}` for polling. Per-ticker flow runs ingest → extract → resolve serially with per-stage `db.commit()` and error capture into `status.errors[]`; ingest summary errors (no CIK, empty submissions, etc) are surfaced too. Frontend adds "Fan out" buttons on both `TickerFilingsCard` and `ThemeFilingsPanel` with 3s-interval polling. New read-path `relationship_context.py` produces `CounterpartyContext` (outbound + inbound grouped by relationship_type, 20-entry cap per bucket); threaded into `node_deep_dive` via `PipelineService._fetch_counterparty_context`. New `RELATIONSHIP_ROUTING = {Business Quality, Risk Assessment, Future Durability}` + `{counterparty_context}` slot in `DEEP_DIVE_USER` with "cite by name, don't re-quote" framing. `_build_counterparty_context` renders `$TICKER` notation for resolved counterparties and an `Mentioned by others` bucket that surfaces the theme-fan-out payoff (e.g., MSFT's prompt sees `$ORCL — competitor`, `$NVDA — other` with no outbound data of its own). Phase 0 bolt-on: Item 1A regex now tolerates mid-word `\s*` (ORCL 10-K went 5.3KB cross-ref fragment → 97KB real prose). Verified: 2-ticker theme fan-out end-to-end via Playwright; NVDA full-stack (3 filings, 2 relationships, 1 auto-resolve); idempotent re-run zero-op.
- Tier 3 v2 Phase D (graph + Supply Chain card + reconciliation): `GET /api/relationships/graph/{ticker}?direction=out|in|both` returns `{root_ticker, nodes[], edges[], summary}` — 1-hop graph with direction, magnitude_pct, bilateral flag. Node identity: resolved via CIK, or normalized name for unresolved counterparties. `tracked` flag annotated from theme seed_tickers. `POST /api/relationships/reconcile` scans all resolved rows for reciprocal pairs (customer↔supplier, partner↔partner, competitor↔competitor, licensor↔licensee, distributor↔reseller, joint_venture↔joint_venture) and flips `confirmed_bilateral=true` on both sides. Frontend: new `SupplyChainEcosystem` component in deep-dive dashboard after Business Quality — shows named counterparties grouped by type, with verbatim quotes, bilateral badges, tracker-link for tracked tickers, and an "Unnamed concentrations" bucket. Verified: ORCL graph returns 6 nodes + 5 edges bucketed by competitor/other/partner
- Tier 3 v2 Phase C (counterparty resolution): new `counterparty_aliases` table + `resolved_to_cik` / `resolved_to_ticker` columns on `relationships`. Normalizer strips corporate suffixes (Inc/Corp/LLC/Holdings/Group/etc) and punctuation. RapidFuzz token_set_ratio against EDGAR's ~10k-entity company_tickers.json — auto-resolves at ≥95, surfaces 80-94 for manual curation. On-demand endpoints `POST /api/relationships/resolve/{ticker}` + `GET /api/relationships/unresolved` + `POST /api/relationships/alias`. Write-through: creating an alias backfills every matching Relationship row. Frontend curation panel on `/filings` shows pending counterparties with top-5 candidates and one-click "Use this" resolution. Verified on ORCL: Microsoft Azure auto-resolved to MSFT, AWS manually resolved to AMZN, AMPR/GOOG/SoftBank correctly surfaced to queue
- Tier 3 v2 Phase B (relationship extraction, on-demand): new `relationships` table + `FilingSection.relationships_extracted_at` tombstone column for idempotency (including zero-relationship sections). Haiku extractor with Pydantic structured output over 15K-char excerpts from `item_1_business`, `item_1a_risk_factors`, `item_7_mda`, `item_2_mda_10q`. On-demand endpoints `POST /api/filings/extract-relationships/{ticker}` (with `?force=true` re-run) + `GET /api/filings/{ticker}/relationships`. Verified end-to-end on ORCL: 5 relationships (Ampere joint-venture 29%, SoftBank partner, AWS/Azure/GCP competitors) with verbatim quotes
- Tier 3 v2 Phase A (heading-trim polish): Item 7 MD&A in 10-Ks now anchors to line-start (MULTILINE) so cross-references like "…see Item 7 Management's Discussion…" no longer out-compete the real heading by body length. Regex tolerates HTML-unwrap "O\s*F" word splits. Added boundary markers for 10-K Items 1B/1C/2/3/4/5/6. Verified on ORCL: Item 7 went from 2K chars of cross-ref fragment → 74K of real MD&A starting with "We begin Management's Discussion…"
- Tier 3 v2 Phase A (prompt routing): `FILING_EXCERPT_ROUTING` map + `_build_filing_excerpt_context` in `node_deep_dive`; filing sections fetched per-ticker in `PipelineService._fetch_filing_sections` and threaded through as kwarg. Section excerpts truncated to 5K chars at prompt-build time. Verified end-to-end on ORCL: Business Quality / Risk Assessment / Growth & Earnings / Management & Governance / Future Durability each receive the relevant 10-K / 10-Q / DEF 14A excerpt; `Financial Health` correctly gets none
- Tier 3 v2 Phase A (frontend): `/filings` page grouped by thesis → ticker → section with modal reader and on-demand "Ingest latest" button
- Tier 3 v2 Phase A (backend): `filing_sections` table + migration; `FilingSection` ORM; `EdgarClient.get_filing_index` + `fetch_document`; BS4 extractor with hybrid boundary-marker regex (capped sections, no bleed); on-demand ingest orchestrator (latest 10-K, 10-Q, DEF 14A per ticker, idempotent); `POST /api/filings/ingest/{ticker}`, `POST /api/filings/ingest/batch`, `GET /api/filings/{ticker}`, `GET /api/filings/{ticker}/{accession}/sections/{section_key}`. Verified end-to-end against AAPL + ORCL
- Tier 1: key-case bug in data_gaps, silent-failure detection, technical/sentiment prompt routing
- Tier 3 v1: EDGAR XBRL ingest — `filings` + `xbrl_facts` tables, whitelist of RPO / debt-maturity / concentration / credit concepts, ticker → CIK → companyfacts → persist
- EDGAR UI surfacing: report API + SSE carry `edgar_facts`; new `DebtMaturityLadder` + `RPOTrend` Recharts components
- SEC citation surfacing: `SEC EDGAR · XBRL filings` appears in Library citations panel
- Chart formatting: shared `formatUSD` (`$X.XXB` / `$X.XM` / `$XK` / `$X,XXX`) applied across all dollar charts; findings/evidence stacked vertically
- Tier 2: FMP analyst grades + grades-historical + grades-consensus + price-target-consensus + ratings-snapshot + insider-trading — routed into `_fmt_fundamentals`
- Prefill-error purge: stale `"does not support assistant message prefill"` CategoryError entries filtered out of aggregation (state JSONB untouched)
- Transaction-collision bug in deep-dive: intra-phase SQL now uses dedicated sessions so `async with db.begin()` persist block stays clean
